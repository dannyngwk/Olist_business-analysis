# Raw data — too large for git, download from Kaggle (see README)
data/raw/*.csv
data/raw/*.zip
data/*.duckdb
*.duckdb
*.duckdb.wal

# Python
.venv/
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.python-version

# Meltano (plugin lockfiles in meltano/plugins/ ARE committed — they make
# installs reproducible and remove the dependency on Meltano Hub at runtime)
meltano/.meltano/
.env
*.env

# dbt
transform/target/
transform/dbt_packages/
transform/logs/
transform/.user.yml

# Dagster
orchestrator/.dagster/
tmp*/
.tmp*/
dagster_home/

# Logs
logs/

# Local-only folders
Team3_Project Olist/
.claude/

# Secrets
*.pem
*.key
credentials.json
.secrets/

# OS / IDE
.DS_Store
.vscode/
.idea/
